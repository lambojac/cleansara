import { AxiosInstance, AxiosResponse } from "axios";
import { CountryType, CardBrand } from "../utils";
interface IAddress {
    city: string;
    country: string;
    postal_code: string;
    state: string;
    street: string;
    street2: string;
}
interface _Identity {
    country: string;
    image?: string;
    number: string;
    type: string;
}
interface CreateCustomerPayload {
    email: string;
    first_name: string;
    last_name: string;
    country: CountryType;
}
interface TierOnePayload {
    customer_id: string;
    phone: {
        phone_number: string;
        phone_short_code: string;
    };
    address: IAddress;
    dob: string;
    identification_number: string;
}
interface TierTwoPayload {
    customer_id: string;
    identity: _Identity;
}
interface IFullEnrollCustomer {
    first_name: string;
    last_name: string;
    email: string;
    phone: {
        phone_number: string;
        phone_country_code: string;
    };
    dob: string;
    identification_number: string;
    identity: _Identity;
    address: IAddress;
}
interface IUpdateCustomer {
    customer_id: string;
    photo: string;
    phone: {
        phone_number: string;
        phone_country_code: string;
    };
    middle_name: string;
    identity: _Identity;
}
export default class Customers {
    private axios;
    constructor(axios: AxiosInstance);
    FullEnrollCustomer(payload: IFullEnrollCustomer): Promise<AxiosResponse | any>;
    CreateCustomer(payload: CreateCustomerPayload): Promise<AxiosResponse | any>;
    UpgradeCustomerTier1(payload: TierOnePayload): Promise<AxiosResponse | any>;
    UpgradeCustomerTier2(payload: TierTwoPayload): Promise<AxiosResponse | any>;
    GetCustomer(customerID: string): Promise<AxiosResponse | any>;
    GetAllCustomers(): Promise<AxiosResponse | any>;
    GetCustomerCards(customerID: string): Promise<AxiosResponse | any>;
    GetCustomerTransactions(customerID: string): Promise<AxiosResponse | any>;
    GetCustomerVirtualAccounts(customerID: string): Promise<AxiosResponse | any>;
    CustomerCardEnrollment(customerID: string, brand: CardBrand): Promise<AxiosResponse | any>;
    UpdateCustomer(payload: IUpdateCustomer): Promise<AxiosResponse | any>;
    SetCustomerBlacklistActive(customerID: string, status: boolean): Promise<AxiosResponse | any>;
}
export {};
