import { AxiosInstance, AxiosResponse } from "axios";
import { CurrencyType, QueryParams } from "../utils";
export default class Wallets {
    private axios;
    constructor(axios: AxiosInstance);
    GetWallets(): Promise<AxiosResponse | any>;
    GetWalletsHistory(params?: QueryParams): Promise<AxiosResponse | any>;
    GetWalletsHistoryByCurrency(currencyCode: CurrencyType, params?: QueryParams): Promise<AxiosResponse | any>;
}
