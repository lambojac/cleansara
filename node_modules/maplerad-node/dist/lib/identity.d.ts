import { AxiosInstance, AxiosResponse } from "axios";
export default class Identity {
    private axios;
    constructor(axios: AxiosInstance);
    VerifyIdentity(bvn: string): Promise<AxiosResponse | any>;
}
